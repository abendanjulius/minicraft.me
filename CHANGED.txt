Eldercube — v2.7.7  (CUMULATIVE: supersedes 2.7.0 - 2.7.6)

THE SPIN: the "no progress" recovery was counting the bot as stuck while it was
parked in range and placing normally. Every ~4.5 s it rose 4 blocks and
re-navigated, then settled back — 13 lurches a minute. Recovery now only runs
while the bot still has to TRAVEL. Verified: 13/min -> 0/min.

  js/pathfind.js   *** NEW ***  A* voxel pathfinder
  js/aitest.js     *** NEW ***  /aitest self-test harness
  js/ai.js         spin fix (above) + tighter approach so it sits on its spot
  js/player.js     held item hidden when it would clip a wall
  js/commands.js   registers /aitest
  js/main.js       APP_VERSION -> 2.7.7
  sw.js            CACHE -> minicraft-v191
  version.json     -> 2.7.7

Upload all eight. js/pathfind.js and js/aitest.js are NEW files.
