Eldercube — v2.7.3  (CUMULATIVE: replaces 2.7.0 / 2.7.1 / 2.7.2)
================================================================
FIXES THE "Game script failed to load" ERROR.

Cause: js/ai.js had a real syntax error — my new `faceHold` variable collided
with an existing one, so the module never parsed and the whole script graph
died. My syntax checker (`node --check`) was silently passing broken files;
I have replaced it with a real ES-module parser and re-verified all 33 files.

Upload ALL of these together. Two are NEW files:

  js/pathfind.js   *** NEW ***  A* voxel pathfinder
  js/aitest.js     *** NEW ***  /aitest self-test harness
  js/ai.js         SYNTAX FIX + A* routing + builder facing
  js/player.js     held items no longer clip through walls
  js/commands.js   registers /aitest
  js/main.js       APP_VERSION -> 2.7.3
  sw.js            CACHE -> minicraft-v187; precaches both new files
  version.json     -> 2.7.3

After upload, confirm js/pathfind.js and js/aitest.js exist in the repo,
then hard-refresh (Ctrl+Shift+R) or use "Clear cache & reload".
