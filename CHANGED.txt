Eldercube — v2.7.6  (CUMULATIVE: supersedes 2.7.0 - 2.7.5)

  js/pathfind.js   *** NEW ***  A* voxel pathfinder
  js/aitest.js     *** NEW ***  /aitest self-test harness
  js/ai.js         - bot now FLIES as the build rises (it was measuring height
                     against the tower it had just built, so it never took off)
                   - cannot place blocks more than ~3 above itself
                   - approaches every block in a course from the SAME side, so
                     it looks directly at its work and turns only per course
  js/player.js     held item is hidden once tucked, so it cannot clip through
  js/commands.js   registers /aitest
  js/main.js       APP_VERSION -> 2.7.6
  sw.js            CACHE -> minicraft-v190
  version.json     -> 2.7.6

Upload all eight. js/pathfind.js and js/aitest.js are NEW files.
